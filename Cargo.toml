[workspace]
resolver = "3"
members = [
    "backend",
    "bevy-front",
    "engine",
    "multiplayer-protocol",
]

[workspace.dependencies]

[profile.release]
opt-level = 2
strip = true
lto = false
panic = "abort"

[profile.dev]
opt-level = 2
debug = 1

[profile.dev.package."*"]
debug = 0
