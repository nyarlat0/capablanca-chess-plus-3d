[package]
name = "multiplayer-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
