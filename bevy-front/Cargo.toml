[package]
name = "bevy-front"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
bevy = { version = "0.19.0", features = ["jpeg"] }
bevy_panorbit_camera = "0.35.0"
capablanca-engine = { path = "../engine" }
